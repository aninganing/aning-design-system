import { Meta } from '@storybook/blocks';
import * as tokens from '@aninganing/tokens';

<Meta title="Foundations/Spacing" />

# Spacing

Spacing scale generated from `core/spacing.json`.

<div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
  {Object.entries(tokens)
    .filter(([name]) => name.toLowerCase().startsWith('space'))
    .map(([name, value]) => (
      <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
        <div style={{ width: String(value), height: 16, background: '#2563eb' }} />
        <code style={{ fontSize: 12 }}>
          {name}: {String(value)}
        </code>
      </div>
    ))}
</div>
