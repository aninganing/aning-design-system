import { Meta } from '@storybook/blocks';
import * as tokens from '@aninganing/tokens';

<Meta title="Foundations/Typography" />

# Typography

Font size scale generated from `core/typography.json`.

<div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
  {Object.entries(tokens)
    .filter(([name]) => name.toLowerCase().startsWith('fontsize'))
    .map(([name, value]) => (
      <div key={name} style={{ display: 'flex', alignItems: 'baseline', gap: 12 }}>
        <span style={{ fontSize: String(value) }}>Aa 가나다</span>
        <code style={{ fontSize: 12, color: '#71717a' }}>
          {name}: {String(value)}
        </code>
      </div>
    ))}
</div>
